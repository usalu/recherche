# Archived legacy research tree

This directory is **historical only**.

- It is not the live source of truth.
- It must not be used as the default input for new imports.
- Neo4j is the current source of truth.
- Any reuse of material from this archive requires an explicit review against the current graph and current import rules.

The archive remains here for provenance and recovery, not for normal editing.
