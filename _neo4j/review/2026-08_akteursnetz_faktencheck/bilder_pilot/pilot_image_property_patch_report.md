# Pilot image property patch report

- Patch rows: **35**
- Overlay-only, not importable: **13**
- Export match errors: **0**
- Live validation: **not run**

No write was performed against Neo4j.

## Overlay-only nodes

- `AT:F01` — overlay node has no canonical graph id
- `AT:I02` — overlay node has no canonical graph id
- `AT:M03` — overlay node has no canonical graph id
- `AT:S01` — overlay node has no canonical graph id
- `AT:U01` — overlay node has no canonical graph id
- `GB:F04` — overlay node has no canonical graph id
- `GB:M14` — overlay node has no canonical graph id
- `GB:N05` — overlay node has no canonical graph id
- `GB:U21` — overlay node has no canonical graph id
- `NL:M06` — overlay node has no canonical graph id
- `NL:N02` — overlay node has no canonical graph id
- `NL:N03` — overlay node has no canonical graph id
- `NL:U07` — overlay node has no canonical graph id
