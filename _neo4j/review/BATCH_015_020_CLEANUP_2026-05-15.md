# Batch 015-020 cleanup — 2026-05-15

## Decision

- Retain the user-confirmed project corpus through `batch_015`.
- Replace the older thin `batch_015` representation with the reviewed `neo4j_requested_cases_batch_v2` package.
- Remove the old current-repo `batch_016`-`batch_020` project set from both the repository and live Neo4j graph.

## Evidence used

- Old `_archive/research/gebaeude/` filename inventory validates all five surviving `batch_015` cases.
- None of the old current-repo `batch_016`-`batch_020` source filenames matched that original `gebaeude/` case inventory.
- The old `batch_016`-`batch_020` tree had weaker provenance: no manifests, no validation reports, and a single bulk-write timestamp.
- A full live-graph backup was created before deletion:
  - `_neo4j/review/backups/20260515T105547Z-mit-bestand/`

## Replacement batch 015

The surviving `batch_015` contains:

- `55_Great_Suffolk_Street_London.md`
- `Association_house_Groeditz.md`
- `Association_house_Plauen.md`
- `AWM_Muenster_Circular_Office.md`
- `BedZED_London_Hackbridge.md`

The replacement package now lives at:

- `_neo4j/intake/archive/2026-05-15_project_batches_legacy/raw_tree/neo4j_batch_015_exports/neo4j_exports/batches/batch_015/`

## Live Neo4j action

- Old late-batch relationships not shared with trusted `001`-`014` were removed.
- Old late-only nodes not retained by the replacement `batch_015` were removed.
- The five reviewed replacement cases were imported.
- Temporary `a_` actor duplicates created during replay were folded back to canonical actor IDs.
- The stale `registry_stub_only` project properties on `p_awm_muenster_circular_office` were replaced with the full reviewed project properties.

Machine-readable cleanup details:

- `_neo4j/review/batch_015_020_cleanup_db_report.json`

## Agent rule

Do not reintroduce the removed old current-repo `batch_016`-`batch_020` set as accepted project corpus data without explicit user approval and a new provenance review.
