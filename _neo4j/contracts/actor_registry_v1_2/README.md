# Actor registry v1.2 intake contract

This contract preserves the schema patch used by the actor-registry transport chunks.

The old chunks are intake artifacts only; accepted records are merged into `processed/actor_registry/` with separate provenance.
