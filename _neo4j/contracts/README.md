# Intake contracts

This directory stores supported input contracts and examples for future drops.

- `project_batches_v1_1/` — inherited project-batch contract from the older export workflow.
- `actor_registry_v1_2/` — retained schema patch and notes for actor-registry inputs.

Contracts describe accepted input shapes. They do not decide graph truth.
