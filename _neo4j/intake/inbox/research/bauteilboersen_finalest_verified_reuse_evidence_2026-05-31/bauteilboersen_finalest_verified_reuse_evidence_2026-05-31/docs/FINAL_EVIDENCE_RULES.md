# Final evidence and import rules

1. Use product/detail URLs where they exist.
2. If product URLs are not public, use category/listing pages.
3. If neither is available, use scope/actor pages only to classify the actor, not to assert specific `mat_*` or `bt_*` relationships.
4. Keep review-only rows out of graph import until a direct URL proves the exact Material or Bauteiltyp.
5. One claim row must have one evidence URL and a quote/line reference or capture note.
6. Conservative mapping rules remain active: no generic-to-specific material conversion without textual support.
